---
// Ein Formular für beide Sprachen. Die versteckten Felder gehen mit: eine
// englische Anfrage darf keine deutsche Autoantwort auslösen, und nach dem
// Absenden muss die englische Dankeseite kommen, nicht die deutsche.
import { SITE, PATHS } from "../consts";
import { lokalisiere } from "../i18n/pages.mjs";
import type { Sprache } from "../i18n/ui";

interface Props {
  lang?: Sprache;
}
const { lang = "de" } = Astro.props;
const en = lang === "en";

const T = {
  betreff: en ? "New enquiry via felix-paul.de" : "Neue Anfrage über felix-paul.de",
  weiter: en ? `${SITE.url}/en/thank-you/` : `${SITE.url}/thank-you/`,
  autoantwort: en
    ? "Thank you for your message via felix-paul.de. I have received your enquiry and usually reply within 48 hours. Best regards, Felix Paul"
    : "Vielen Dank für Ihre Nachricht über felix-paul.de. Ich habe Ihre Anfrage erhalten und melde mich in der Regel innerhalb von 48 Stunden zurück. Beste Grüße, Felix Paul",
  name: en ? "Name *" : "Name *",
  namePlatzhalter: en ? "Jane Doe" : "Max Mustermann",
  email: en ? "Email *" : "E-Mail *",
  emailPlatzhalter: en ? "jane.doe@example.com" : "max.mustermann@gmail.com",
  nachricht: en ? "Message *" : "Nachricht *",
  nachrichtPlatzhalter: en ? "What can I help you with?" : "Worum geht es? ",
  senden: en ? "Send enquiry" : "Anfrage senden",
  direkt: en ? "Or write to me directly:" : "Oder direkt schreiben:",
};
---

<!-- Ohne Kasten: das Formular steht auf der Fläche des Abschnitts, die
     Felder selbst tragen die Struktur. Ein Rahmen um einen Rahmen war eine
     Kante zu viel. -->
<form
  action={`https://formsubmit.co/${SITE.email}`}
  method="POST"
  class="space-y-6"
>
  <input type="hidden" name="_subject" value={T.betreff} />
  <input type="hidden" name="_template" value="table" />
  <input type="hidden" name="_captcha" value="true" />
  <input type="hidden" name="_next" value={T.weiter} />
  <input
    type="hidden"
    name="_autoresponse"
    value={T.autoantwort}
  />
  <input type="text" name="_honey" tabindex="-1" autocomplete="off" class="hidden" aria-hidden="true" />

  <div>
    <label for="name" class="block text-xs font-semibold uppercase tracking-wider text-ink-500">{T.name}</label>
    <input
      type="text"
      id="name"
      name="name"
      placeholder={T.namePlatzhalter}
      required
      autocomplete="name"
      class="mt-2 min-h-11 w-full rounded-lg border border-ink-300 bg-surface px-3.5 py-2.5 text-[0.9375rem] text-ink-900 transition placeholder:text-ink-400 focus:border-brand-500 focus:outline-none"
    />
  </div>

  <div>
    <label for="email" class="block text-xs font-semibold uppercase tracking-wider text-ink-500">{T.email}</label>
    <input
      type="email"
      id="email"
      name="email"
      required
      autocomplete="email"
      placeholder={T.emailPlatzhalter}
      class="mt-2 min-h-11 w-full rounded-lg border border-ink-300 bg-surface px-3.5 py-2.5 text-[0.9375rem] text-ink-900 transition placeholder:text-ink-400 focus:border-brand-500 focus:outline-none"
    />
  </div>

  <div>
    <label for="message" class="block text-xs font-semibold uppercase tracking-wider text-ink-500">{T.nachricht}</label>
    <textarea
      id="message"
      name="message"
      rows="5"
      required
      placeholder={T.nachrichtPlatzhalter}
      class="mt-2 w-full rounded-lg border border-ink-300 bg-surface px-3.5 py-2.5 text-[0.9375rem] leading-relaxed text-ink-900 transition placeholder:text-ink-400 focus:border-brand-500 focus:outline-none"
    ></textarea>
  </div>

  <div class="flex items-start gap-3 border-t border-ink-200 pt-6 text-sm leading-relaxed text-ink-600">
    <input
      id="privacy"
      type="checkbox"
      name="privacy"
      value="yes"
      required
      class="mt-1 h-4 w-4 shrink-0 rounded border-ink-300 text-brand-700"
    />
    <label for="privacy">
      {
        en ? (
          <Fragment>
            I have read the{" "}
            <a href={lokalisiere(PATHS.datenschutz, lang)} class="text-brand-700 underline underline-offset-2">
              privacy policy
            </a>{" "}
            and consent to my details being processed in order to handle this enquiry.
          </Fragment>
        ) : (
          <Fragment>
            Ich habe die{" "}
            <a href={PATHS.datenschutz} class="text-brand-700 underline underline-offset-2">
              Datenschutzerklärung
            </a>{" "}
            gelesen und willige ein, dass meine Angaben zur Bearbeitung der Anfrage verarbeitet werden.
          </Fragment>
        )
      }
    </label>
  </div>

  <button
    type="submit"
    class="inline-flex min-h-11 w-full items-center justify-center rounded-full bg-brand-700 px-7 text-[0.9375rem] font-medium text-white transition hover:bg-brand-800 sm:w-auto"
  >
    {T.senden}
  </button>

  <p class="text-xs text-ink-500">
    {T.direkt} <a class="text-brand-700 underline underline-offset-2" href={`mailto:${SITE.email}`}>{SITE.email}</a>
  </p>
</form>
